import React from "react"
// import { getUser } from "./services/auth"

const Profile = () => (
  <>
    <ul>
      {/* <li>Name: {getUser().name}</li>
      <li>E-mail: {getUser().email}</li> */}
      <p style={{ textAlign: "center" }}>No video content at the moment.</p>
    </ul>
  </>
)

export default Profile
